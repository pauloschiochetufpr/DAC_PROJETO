server:
  port: 8080
spring:
  application:
    name: auth-service
  data:
    mongodb:
      uri: mongodb://mongo:27017/auth_db
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include: health

jwt:
  secret: 'lç\\|fo,.,jwief:>23(*&{+{=§}})'
  expiration: 600000 # 600000ms = 10 minutos

